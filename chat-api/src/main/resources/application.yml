spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chatapps
    username: root
    password:
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    generate-ddl: true
